# Report Skeleton

## 用途
- 用于周报、阶段性报告、里程碑总结。
- 默认采用三阶段语义：`刚接手时` / `已完成` / `正在做或后续计划`。

## 推荐结构

## 1. 阶段化结论

### 阶段一：刚接手任务时的情况
- 基线能力：
- 已知限制：
- 主要阻断：

### 阶段二：已完成的情况
- 已完成能力提升：
- 关键实现：
- 验证结果：

### 阶段三：正在做/后续计划
- 当前在推进：
- 下一步计划：
- 剩余边界与风险：

## 2. 函数级关键改动（文件 + 函数）

- 文件：`<path>`
- 函数：`<function or pattern::method>`
- 作用：
- 关键逻辑：

- 文件：`<path>`
- 函数：`<function or pattern::method>`
- 作用：
- 关键逻辑：

## 3. 关键节点一键命令

### 节点一（刚接手时基线路径）
```bash
<copy-paste command>
```

### 节点二（已完成里程碑路径）
```bash
<copy-paste command>
```

### 节点三（正在做/后续路径）
```bash
<copy-paste command>
```

## 4. 本周风险与阻塞（可选）
- 风险项：
- 阻塞项：
- 需要协同：

## 5. 下周计划（可选）
- P0：
- P1：
- 验收标准：

## 6. 附录（可选）
- 关联报告：
- 关联测试：
- 关联实验文件：

## 写作规则
- 用事实句，不用类比。
- 所有关键结论都尽量可定位到 `文件 + 函数`。
- 命令要可复制、可执行；若未实测，明确标注 `未实测`。
